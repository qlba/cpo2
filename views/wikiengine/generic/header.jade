//
   Created by qlba on 10.11.2016.

header.Container#pageHeader
    header.Container#mainLogoHeader
        p
            span.LogoSpec W
            | IKI
            span.LogoSpec E
            | NGINE
    div.Container
        section.Container.ActionBox
            if(login === undefined)
                form(action="/wiki/sys/login", method="get")
                    p
                        b Not
                        | logged in
                    input(type="hidden", name="return", value="/wiki/article?article=" + title)
                    input(type="submit", value="Log in / Sign up")
            else
                form(action="/wiki/sys/login", method="get")
                    p Logged in as
                        b qlba
                    input(type="hidden", name="return", value="/wiki/article?article=" + title)
                    input(type="submit", value="Log out")
        | |
        section.Container.ActionBox
            form(action="/wiki/article", method="get")
                input(type="search", name="article", autocomplete="on",
                placeholder="Search article...")
                input(type="submit", value="Search")
